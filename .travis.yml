sudo: required
language: java

services:
  - docker
jdk:
  - oraclejdk8

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    mvn dockerfile:build -DpushImage;
    fi